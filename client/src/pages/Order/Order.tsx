import { OrderSection } from "components/OrderSection";
import { ContainerStyled } from "styled";

export const Order = () => {
  return (
    <ContainerStyled>
      <OrderSection />
    </ContainerStyled>
  );
};
